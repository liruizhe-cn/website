---
// No props needed for now
---

<section class="resume-section scroll-reveal-animate">
  <h2>Education</h2>
  <div class="education-entry">
    <div class="header">
      <span class="institution">
        <strong>Nantong University</strong><br />
        Japanese Major, <em>Undergraduate</em>
      </span>
      <span class="date">September 2025 – Present</span>
    </div>
  </div>

  <div class="education-entry">
    <div class="header">
      <span class="institution">
        <strong>No.1 Senior High School of Zhongmu County</strong><br />
        <em>Graduated</em>
      </span>
      <span class="date">August 2022 – June 2025</span>
    </div>
  </div>
</section>

<style>
  .resume-section {
    margin-bottom: 4rem;
  }
  h2 {
    font-size: clamp(1.8rem, 1.5rem + 1.5vw, 2.8rem);
    margin-bottom: 2rem;
    font-weight: 600;
    border-bottom: 1px solid var(--text-muted);
    padding-bottom: 0.5rem;
  }
  .education-entry .header {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    flex-wrap: wrap; /* Allow wrapping on smaller screens */
    margin-bottom: 0.8rem;
    font-size: 1.2rem;
  }
  .education-entry {
    margin-bottom: 1.5rem;
  }
  .education-entry:last-child {
    margin-bottom: 0;
  }
  .education-entry .institution {
    margin-right: 1rem; /* Space between institution and date */
  }
  .education-entry .date {
    font-family: var(--font-family-sans);
    color: var(--text-muted);
    font-size: 1rem;
    white-space: nowrap; /* Prevent date from wrapping */
  }
  .education-entry .details {
    list-style: none;
    padding-left: 0;
    margin-left: 1rem; /* Indent details slightly */
    font-size: 1.1rem;
    line-height: 1.6;
  }
  .education-entry .details li {
    margin-bottom: 0.3rem;
  }

  @media (max-width: 600px) {
    .education-entry .header {
      flex-direction: column; /* Stack header items vertically */
      align-items: flex-start;
    }
    .education-entry .date {
      margin-top: 0.3rem;
    }
    .education-entry .details {
      margin-left: 0; /* Remove indent on mobile */
    }
  }
</style>
