<div
  id="dynamic-background-container"
  aria-hidden="true"
  transition:persist={true}
>
  <div class="background-wash"></div>
  <div class="background-orb orb-one"></div>
  <div class="background-orb orb-two"></div>
  <div class="background-orb orb-three"></div>
</div>

<style>
  #dynamic-background-container {
    position: fixed;
    inset: -16vmax;
    z-index: -1;
    overflow: hidden;
    opacity: var(--dynamic-bg-opacity);
    contain: strict;
    pointer-events: none;
    transform: translateZ(0);
  }

  .background-wash,
  .background-orb {
    position: absolute;
    will-change: transform;
    transform: translateZ(0);
  }

  .background-wash {
    inset: 0;
    background:
      radial-gradient(
        circle at 15% 25%,
        rgba(103, 211, 150, 0.8),
        transparent 34%
      ),
      radial-gradient(
        circle at 78% 20%,
        rgba(78, 193, 220, 0.72),
        transparent 36%
      ),
      radial-gradient(
        circle at 55% 82%,
        rgba(230, 190, 72, 0.68),
        transparent 38%
      );
    filter: blur(11vmax) saturate(118%);
    animation: wash-drift 24s ease-in-out infinite alternate;
  }

  .background-orb {
    width: 58vmax;
    height: 58vmax;
    border-radius: 50%;
    filter: blur(8vmax);
    opacity: 0.56;
    mix-blend-mode: screen;
  }

  .orb-one {
    top: 2%;
    left: 4%;
    background: #34c875;
    animation: orb-one-drift 28s ease-in-out infinite alternate;
  }

  .orb-two {
    top: 8%;
    right: 0;
    background: #22a8ca;
    animation: orb-two-drift 31s ease-in-out infinite alternate;
  }

  .orb-three {
    right: 18%;
    bottom: 0;
    background: #e0bb36;
    animation: orb-three-drift 34s ease-in-out infinite alternate;
  }

  @keyframes wash-drift {
    from {
      transform: translate3d(-2%, -1%, 0) scale(1);
    }
    to {
      transform: translate3d(3%, 2%, 0) scale(1.06);
    }
  }

  @keyframes orb-one-drift {
    from {
      transform: translate3d(-4%, -2%, 0) scale(0.92);
    }
    to {
      transform: translate3d(18%, 12%, 0) scale(1.08);
    }
  }

  @keyframes orb-two-drift {
    from {
      transform: translate3d(8%, -8%, 0) scale(1.04);
    }
    to {
      transform: translate3d(-14%, 14%, 0) scale(0.9);
    }
  }

  @keyframes orb-three-drift {
    from {
      transform: translate3d(-8%, 10%, 0) scale(0.94);
    }
    to {
      transform: translate3d(12%, -12%, 0) scale(1.08);
    }
  }

  @media (max-width: 767px) {
    #dynamic-background-container {
      inset: -24vmax;
    }

    .background-wash {
      filter: blur(14vmax) saturate(110%);
      animation-duration: 34s;
    }

    .background-orb {
      width: 68vmax;
      height: 68vmax;
      filter: blur(12vmax);
      opacity: 0.42;
      animation-duration: 40s;
    }

    .orb-three {
      display: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .background-wash,
    .background-orb {
      animation: none;
    }
  }
</style>
